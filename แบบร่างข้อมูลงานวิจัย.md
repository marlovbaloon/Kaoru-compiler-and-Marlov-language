
# การคืนพื้นที่สแต็กเชิงประจักษ์โดยขับเคลื่อนด้วย AST สำหรับสถาปัตยกรรม ARM Cortex-M บนระบบ Bare-Metal

**ผู้เขียน:** อาศิระ "Marlov" ลายแพ่ง  
**สังกัด:** ภาควิชาวิศวกรรมคอมพิวเตอร์  
**อีเมล:** marlovbaloon@gmail.com  

---

### บทคัดย่อ
ระบบฝังตัวแบบ Bare-metal ที่ทำงานบนไมโครคอนโทรลเลอร์ สถาปัตยกรรม ARM Cortex-M ดำเนินการภายใต้ข้อจำกัด SRAM ที่เข้มงวดอย่างยิ่ง โดยการเติบโตของสแต็กที่ไม่สามารถคาดเดาได้ก่อให้เกิดความเสี่ยงร้ายแรงต่อการสูญเสียความถูกต้องของหน่วยความจำและความไม่เสถียรของระบบ คอมไพเลอร์แบบดั้งเดิมพึ่งพาการวิเคราะห์คงที่ในส่วน Back-end และหลักการวิเคราะห์กราฟการไหลของโปรแกรม (CFG) เพื่ออนุมานวงจรชีวิตของตัวแปรท้องถิ่น หรือบังคับใช้กระบวนการทำความสะอาดขณะรันไทม์ เช่น Landing pads ของ C++ RAII บทความนี้นำเสนอกลไกการคืนพื้นที่สแต็กเชิงประจักษ์ที่ขับเคลื่อนด้วยต้นไม้ไวยากรณ์นามธรรม (AST) ซึ่งได้รับการพัฒนาภายใน Kaoru Compiler โดยการกำหนดให้ไวยากรณ์ขอบเขต (Scope Syntax) ในส่วน Front-end เป็นองค์กรหลักในการระบุเจตนาการคืนหน่วยความจำ Kaoru จึงสามารถข้ามการปรับแต่งที่ไม่แน่นอนของส่วน Back-end และแปลงการสิ้นสุดขอบเขตไปเป็นคำสั่ง ARM Thumb-2 (`ADD SP, SP, #N`) โดยตรง เราได้รูปนัยกลไกนี้ผ่านสมานตศาสตร์เชิงปฏิบัติการโครงสร้าง (Structural Operational Semantics) โดยคำนึงถึงกฎการจัดเรียงสแต็กขนาด 8 ไบต์ตามมาตรฐาน ARM AAPCS อย่างชัดเจน พร้อมกลไกจัดการกรณีขอบเขตพิเศษ (Edge Cases) และพิสูจน์คุณสมบัติคงตัวของสแต็กแบบกำหนดได้ เราสมมติฐานว่าการคืนพื้นที่โดยขับเคลื่อนด้วย AST เชิงประจักษ์ช่วยให้บรรลุขอบเขตหน่วยความจำแบบกำหนดได้ กำจัดภาระทางเวลาของ Landing pad และลดขนาดความลึกสแต็กสูงสุดลงในขณะที่ยังคงความสามารถในการคาดเดาของการประมวลผลไว้

คอมไพเลอร์ระดับอุตสาหกรรมอย่าง GCC ถูกนำมาใช้เป็น Toolchain มาตรฐานสำหรับไมโครคอนโทรลเลอร์อย่างแพร่หลาย อันเนื่องมาจากปัจจัยทางเศรษฐศาสตร์ของซอฟต์แวร์ อย่างไรก็ตาม ปรัชญาการออกแบบของ GCC ตั้งอยู่บนสมการของระบบ Desktop/Server ที่เน้นการเพิ่ม Throughput สูงสุด การที่ GCC พึ่งพาการวิเคราะห์กราฟการไหล (CFG) และการระบายสีช่องสแต็กในส่วน Back-End นำมาซึ่งพฤติกรรมที่ไม่แน่นอนในการบริหารจัดการเฟรมสแต็กบนระบบ Bare-Metal การเปลี่ยนแปลงโครงสร้างโค้ดเพียงเล็กน้อยอาจส่งผลให้เค้าโครงเฟรมสแต็กเปลี่ยนไปอย่างผันผวน ทำลายความเป็นระบบกำหนด (Determinism) ซึ่งเป็นหัวใจสำคัญของสถาปัตยกรรมฝังตัวที่มีข้อจำกัดด้าน SRAM Kaoru Compiler จึงถูกออกแบบและพัฒนาขึ้นมาเพื่อโต้แย้งกระบวนทัศน์ดังกล่าว

**คำสำคัญ:** คอมไพเลอร์, ต้นไม้ไวยากรณ์นามธรรม, การคืนพื้นที่สแต็ก, ความเป็นระบบกำหนดของหน่วยความจำ, ระบบฝังตัว, ARM Cortex-M, Bare-Metal

---

### 1. บทนำ (Introduction)
ไมโครคอนโทรลเลอร์ที่ทำงานในสภาวะแวดล้อมฝังตัวที่เน้นความปลอดภัยสูง เช่น สถาปัตยกรรม ARM Cortex-M มักมีโครงสร้างหน่วยความจำแบบไม่สมมาตรอย่างมาก โดยมีหน่วยความจำเข้าถึงโดยสุ่มแบบคงที่ (SRAM) ขนาดเล็กมากเมื่อเทียบกับหน่วยความจำแฟลชสำหรับอ่านอย่างเดียว ในสภาวะแวดล้อมเช่นนี้ การจัดสรรหน่วยความจำพลวัต (`malloc` / `free`) ถูกห้ามใช้อย่างเข้มงวดเนื่องจากความล่าช้าในการประมวลผลที่ไม่แน่นอนและการแตกกระจายของฮีป ด้วยเหตุนี้ โครงสร้างข้อมูลท้องถิ่นจึงพึ่งพาการจัดสรรสแต็กแบบอัตโนมัติเป็นหลัก แม้ว่าคอมไพเลอร์อุตสาหกรรมสมัยใหม่อย่าง LLVM และ GCC จะมีความโดดเด่นสำหรับการประมวลผลทั่วไป แต่การพึ่งพาการอนุมาน CFG ในส่วน Back-end ที่ไม่แน่นอนนั้นไม่เหมาะสมสำหรับสภาวะแวดล้อม Bare-metal พลังงานต่ำพิเศษและเน้นความปลอดภัยสูง (เช่น อุปกรณ์การแพทย์ฝังในร่างกาย หรือหน่วยควบคุมอิเล็กทรอนิกส์ในยานยนต์) ในเครื่องกระตุ้นหัวใจ ความไม่แน่นอนในขณะรันไทม์ถือเป็นความเสี่ยงที่ไม่สามารถยอมรับได้

Kaoru แสดงให้เห็นว่าการย้ายเจตนาการคืนหน่วยความจำมาไว้ที่จุดสะกิดขอบเขต AST ในส่วน Front-end โดยตรง มักให้ทางเลือกที่กระชับ กำหนดได้ และไม่มีภาระรันไทม์เพิ่มขึ้น ซึ่งออกแบบมาเป็นพิเศษสำหรับสถาปัตยกรรม ARM Cortex-M ที่มีข้อจำกัดด้าน SRAM กรอบการทำงานนี้มุ่งเป้าไปที่สภาวะแวดล้อม Bare-metal ที่มีการทำงานแบบจำกัดและไม่มีการเรียกตัวเองซ้ำอย่างชัดเจน โดยเจตนาเว้นการคลี่ขยายข้อยกเว้นแบบพลวัตออกไปเพื่อรับประกันความเป็นระบบกำหนดในขั้นตอนคอมไพล์ แทนที่จะนำการแก้ไขย้อนหลังมาใช้กับสมานตศาสตร์ C แบบเดิม การออกแบบภาษาเฉพาะทางสำหรับ Bare-metal นำเสนอ zero-cost abstraction ที่บังคับใช้การคืนหน่วยความจำแบบกำหนดได้ที่แหล่งกำเนิด (AST) โดยมีความซับซ้อนของคอมไพเลอร์น้อยที่สุด

#### 1.1 ภูมิหลังและข้อจำกัดหน่วยความจำในระบบฝังตัว
บนไมโครคอนโทรลเลอร์ ARM Cortex-M แบบ Bare-metal ตัวชี้สแต็กหลัก (MSP) และตัวชี้สแต็กกระบวนการ (PSP) ทำหน้าที่จัดการเฟรมการทำงานท้องถิ่น เนื่องจากสถาปัตยกรรมเหล่านี้ขาดฮาร์ดแวร์จัดการหน่วยความจำเสมือน (MMU) เหตุการณ์สแต็กล้น (Stack Overflow) จึงเขียนทับเรจิสเตอร์ของอุปกรณ์ต่อพ่วงหรือเซกเมนต์ข้อมูลคงที่โดยตรง ส่งผลให้เกิดการสูญเสียความถูกต้องของข้อมูลโดยไม่แจ้งเตือน เกิด Hard Faults หรือระบบล้มเหลวโดยสิ้นเชิง การรักษาความเป็นระบบกำหนดอย่างสมบูรณ์ในความลึกสแต็กกรณีเลวร้ายที่สุด ($\mathrm{Stack}_{\mathrm{Peak}}$) จึงเป็นข้อกำหนดสำคัญสำหรับมาตรฐานความปลอดภัยแบบเรียลไทม์ (เช่น ISO 26262 และ DO-178C)

#### 1.2 ข้อจำกัดของโมเดลคอมไพเลอร์แบบดั้งเดิม
คอมไพเลอร์อุตสาหกรรม C/C++ สมัยใหม่ (เช่น GCC และ LLVM) แยกส่วนวิเคราะห์ภาษา Front-end ออกจากการสร้างโค้ด Back-end ผ่านรูปแบบตัวแทนกลาง (IR) ภายใต้กระบวนการคอมไพล์มาตรฐาน:
* **พฤติกรรม Front-End แบบไม่ปรับแต่ง (`GCC -O0`):** คอมไพเลอร์จะสร้างคำสั่งจัดสรรสแต็กเมื่อเริ่มฟังก์ชันและคงตัวแปรที่ประกาศไว้ทั้งหมดภายในเฟรมสแต็กจนกว่าฟังก์ชันจะคืนค่า โดยไม่คำนึงถึงขอบเขตขอบเขตทางไวยากรณ์จริง ส่งผลให้เกิดเฟรมสแต็กขยายใหญ่และใช้หน่วยความจำสูง
* **การปรับแต่งแบบอนุมานใน Back-End (`GCC -O2`):** ส่วน Middle/Back-end ปรับแต่งการใช้ช่องสแต็กโดยใช้การวิเคราะห์กราฟการไหลของโปรแกรม (CFG) และอัลกอริทึมการระบายสีช่องสแต็ก อย่างไรก็ตาม การอนุมานวงจรชีวิตนี้ไม่แน่นอนเมื่อเทียบกับสมานตศาสตร์ซอร์สโค้ดใน Front-end การเปลี่ยนแปลงโครงสร้างโค้ดเพียงเล็กน้อยสามารถเปลี่ยนเค้าโครงเฟรมแอสเซมบลีที่สร้างขึ้นได้อย่างมาก
* **โครงสร้างการทำความสะอาดขณะรันไทม์ (C++ RAII):** C++ Resource Acquisition Is Initialization (RAII) ผูกการทำความสะอาดตัวแปรกับการออกจากขอบเขตผ่าน Destructors อย่างไรก็ตาม โมเดลการ 구현มาตรฐานนำมาซึ่ง Landing pads, ตารางจัดการข้อยกเว้น และภาระคำสั่งสาขาที่เพิ่มขนาดโค้ดและทำให้เวลาประมวลผลคำสั่งไม่แน่นอน

#### 1.3 กระบวนทัศน์ของ Kaoru: Shift-Left Memory Intent
เพื่อก้าวข้ามความไม่สามารถคาดเดาได้ของการอนุมานวงจรชีวิตใน Back-end เรานำเสนอการออกแบบของ Kaoru Compiler Kaoru ย้ายเจตนาการจัดการหน่วยความจำทั้งหมดไปยัง Front-end ของคอมไพเลอร์ โดยใช้โครงสร้างไวยากรณ์ที่ชัดเจน (เช่น `{ @int x = 10; };`) ต้นไม้ไวยากรณ์นามธรรม (AST) จะทำหน้าที่เป็นจุดสะกิดโดยตรงและมีอำนาจในการคืนพื้นที่สแต็ก แทนที่จะมอบหมายการวิเคราะห์วงจรชีวิตให้กับกระบวนการ Back-end แบบพลวัต Front-end ของ Kaoru จะสั่งให้ตัวสร้างโค้ดออกคำสั่งปรับแต่งสแต็กทันที (`ADD SP, SP, #N`) ณ จุดออกจากขอบเขตทางไวยากรณ์ที่แม่นยำ

#### 1.4 สิ่งที่บทความนี้นำเสนอ (Contributions)
บทความนี้นำเสนอข้อเสนอแนะหลักและการประเมินดังต่อไปนี้:
1. รูปนัยของการคืนพื้นที่สแต็กเชิงประจักษ์ที่ขับเคลื่อนด้วย AST สำหรับสถาปัตยกรรมที่มีข้อจำกัดด้านทรัพยากร โดยใช้สมานตศาสตร์เชิงปฏิบัติการโครงสร้างและข้อจำกัดการจัดเรียงฮาร์ดแวร์
2. กลไกการจัดการกรณีขอบเขตพิเศษ (Edge Cases Handling) ในระดับ Front-end/IR เพื่อป้องกัน Stack Fault, Stack Leak และ Register Encoding Limits
3. การออกแบบสถาปัตยกรรมของไปป์ไลน์ Kaoru Compiler ซึ่งแสดงการลดรูปโดยตรงจากไวยากรณ์ไปสู่ IR และแอสเซมบลี จากจุดสะกิดขอบเขต AST ไปยังคำสั่ง ARM Thumb-2
4. พิสูจน์ทางคณิตศาสตร์ของคุณสมบัติคงตัวในการอนุรักษ์สแต็กและข้อจำกัดการจัดเรียงฮาร์ดแวร์ขนาด 8 ไบต์บนระบบ Bare-metal ผ่านระบบ 7 สมการรูปนัย

---

### 2. งานที่เกี่ยวข้อง (Related Work)

ความปลอดภัยของหน่วยความจำและการปรับแต่งสแต็กได้รับการศึกษาวิจัยอย่างกว้างขวางในคอมไพเลอร์ระดับเดสก์ท็อปและระบบ แต่ข้อจำกัดของระบบฝังตัวแบบ Bare-metal ก่อให้เกิดความท้าทายเฉพาะตัว

#### 2.1 RAII และการจัดการตามขอบเขตใน C++
C++ ใช้ RAII เพื่อจัดการวงจรชีวิตของทรัพยากรโดยอัตโนมัติ เมื่อวัตถุออกจากขอบเขตทางไวยากรณ์ คอมไพเลอร์จะรับประกันการเรียกใช้งาน Destructor อย่างไรก็ตาม บนระบบฝังตัว RAII จะสร้างคำสั่งสาขาและการซ่อน Landing pads เพื่อจัดการสถานการณ์การคลี่ขยาย (Unwinding) ที่อาจเกิดขึ้น การศึกษาเกี่ยวกับ C++ สำหรับระบบฝังตัวแสดงให้เห็นว่า Destructor landing pads เพิ่มขนาดโค้ดและความแปรปรวนของรอบการประมวลผล ทำให้ไม่เหมาะสมสำหรับระบบเรียลไทม์เข้มงวด

#### 2.2 วงจรชีวิตของ Rust และการตรวจสอบการยืม (Borrow Checking)
ภาษาโปรแกรม Rust ให้การรับประกันความปลอดภัยของหน่วยความจำในขั้นตอนคอมไพล์ที่แข็งแกร่งผ่านตัวตรวจสอบการยืมและรูปแบบตัวแทนกลางระดับกลาง (MIR) Rust ติดตามความเป็นเจ้าของตัวแปรและแทรก Drop flags รวมถึง Drop glues ณ จุดออกจากขอบเขตโดยอัตโนมัติ แม้ว่า Rust จะบรรลุ Zero-cost abstractions บนระบบเดสก์ท็อปประสิทธิภาพสูง แต่รูปแบบที่ลดรูปแล้วอาจนำมาซึ่งภาระขนาดโค้ดบนระบบเป้าหมายที่มีข้อจำกัด เว้นแต่จะใช้กระบวนการปรับแต่งโดยอาศัยข้อมูลโปรไฟล์เฉพาะทาง

#### 2.3 การระบายสีช่องสแต็กใน Back-End
การปรับแต่งคอมไพเลอร์ในส่วน Back-end เช่น การระบายสีช่องสแต็กของ GCC ทำการวิเคราะห์ช่วงชีวิตของตัวแปรโดยใช้การวิเคราะห์ Liveness บน CFG ตัวแปรที่มีช่วงชีวิตไม่ซ้อนทับกันจะใช้ระยะออฟเซตสแต็กเดียวกัน แม้ว่าจะช่วยลดขนาดสแต็กรวมได้อย่างมีประสิทธิภาพ แต่กระบวนการนี้ซับซ้อนและไม่โปร่งใสต่อวิศวกรซอฟต์แวร์ ทั้งยังไวต่อแฟล็กการปรับแต่งของคอมไพเลอร์ ซึ่งทำลายความเป็นระบบกำหนดทางโครงสร้างระหว่างซอร์สโค้ดและไบนารีที่สร้างขึ้น

---

### 3. กลไกการคืนพื้นที่ซึ่งขับเคลื่อนด้วย AST (AST-Driven Reclamation Mechanism)

ในส่วนนี้ เรานำเสนอสมานตศาสตร์เชิงโครงสร้าง ไปป์ไลน์การคอมไพล์ และกลไกการดักจับ Edge Cases สำหรับ Kaoru Compiler

#### 3.1 สถาปัตยกรรมไปป์ไลน์ของคอมไพเลอร์
แตกต่างจากคอมไพเลอร์แบบหลายรอบดั้งเดิมที่การวิเคราะห์วงจรชีวิตเกิดขึ้นในระยะหลังของไปป์ไลน์การปรับแต่ง Kaoru สร้างการจับคู่โดยตรงระหว่างบล็อกขอบเขตทางไวยากรณ์และตัวชี้สแต็กของเครื่องเป้าหมาย ไปป์ไลน์ประกอบด้วย 4 ขั้นตอนที่กระชับ:

* **Lexer & AST Parser:** สังเคราะห์เครื่องหมายขอบเขตเชิงประจักษ์ (`{...}`) เข้าสู่โหนดพิเศษ `AST_SCOPE_BLOCK` ระหว่างการประมวลผลโทเคน
* **Scope Trigger Detection:** ท่องไปใน AST และกำหนดออฟเซตไบต์สำหรับตัวแปรท้องถิ่นที่ประกาศภายในเฟรมขอบเขต
* **High-Level IR Generation:** ลดรูปขอบเขต AST เข้าสู่สถิตยศาสตร์ขอบเขตสแต็กใน IR โดยไม่สูญเสียเจตนาทางโครงสร้าง
* **ARM Thumb-2 Direct Emitter:** แปลงโหนดการออกจากขอบเขตโดยตรงเป็นคำสั่งคืนพื้นที่สแต็ก (`ADD SP, SP, #N`)

```text
[ซอร์สโค้ด]: { @int x; }; 
    └──> [Lexer & Parser]: สร้างโครงสร้าง AST Node (AST_SCOPE_BLOCK)
            └──> [AST Scope Trigger]: ตรวจจับจุดขอบเขตและการจัดสรร (N bytes)
                    └──> [High-Level IR]: IR_ENTER_SCOPE / IR_EXIT_SCOPE
                            └──> [Thumb-2 Emitter]: ADD SP, SP, #N

```

#### 3.2 สมานตศาสตร์ไวยากรณ์และการจับคู่โค้ด

ใน Kaoru ไวยากรณ์ขอบเขตเชิงประจักษ์จะกำหนดวงจรชีวิตที่แน่นอนของการจัดเก็บข้อมูลท้องถิ่น พิจารณาโครงสร้างโค้ดดังต่อไปนี้:

```c
// ขอบเขตฟังก์ชันภายนอก
void process_data() {
    // ขอบเขตเชิงประจักษ์ภายใน
    {
        @int x = 10;
        @int y = 20;
        // ตรรกะทางธุรกิจ
    }; // จุดสะกิด AST: คืนพื้นที่ทันที
}

```

เมื่อทำการวิเคราะห์วงเล็บเปิดของขอบเขตภายใน `{` คอมไพเลอร์จะคำนวณขนาดการจัดสรรเฟรมที่ต้องการ ($N = \mathrm{sizeof}(x) + \mathrm{sizeof}(y) = 8\text{ bytes}$) และส่งออกคำสั่งจัดสรร:

```assembly
SUB SP, SP, #8

```

เมื่อวิเคราะห์พบสัญลักษณ์สิ้นสุดขอบเขต `};` โหนด `AST_SCOPE_BLOCK` จะบังคับให้ตัวสร้างโค้ดส่งออกคำสั่งคืนพื้นที่สแต็กที่ตรงกันทันที:

```assembly
ADD SP, SP, #8

```

การจับคู่โดยตรงนี้ช่วยให้มั่นใจได้ว่าพื้นที่สแต็กที่จัดสรรสำหรับตัวแปร x และ y จะถูกคืนทันที ณ ขอบเขตทางไวยากรณ์ แทนที่จะค้างไว้นานตลอดระยะเวลาการทำงานของ `process_data()`

#### 3.3 การดักจับกรณีขอบเขตพิเศษและการควบคุมภาวะสุ่มเสี่ยง (Edge Cases & Corner-Case Handling)

เพื่อรับประกันความสมบูรณ์เชิงโครงสร้าง (Structural Integrity) และป้องกันข้อผิดพลาดร้ายแรงขณะรันไทม์ ไปป์ไลน์ส่วน AST Scope Detection และ Code Generation ของ Kaoru ถูกออกแบบให้ดักจับ Edge Cases สำคัญ 5 ประการดังนี้:

1. **ขอบเขตว่างเปล่าที่ไม่มีการจัดสรรหน่วยความจำ (Empty Scope Mitigation):**
หากบล็อกไวยากรณ์เปิด-ปิด `{ }` ไม่มีการประกาศตัวแปรท้องถิ่น ฟังก์ชันคำนวณขนาดเฟรมจะได้ $N = 0$ ในกรณีนี้ IR Emitter จะทำการระงับ (Suppress) การส่งออกคำสั่ง `SUB SP, SP, #0` และ `ADD SP, SP, #0` โดยสิ้นเชิง ส่งผลให้เกิด Zero Instruction Overhead ต่อการทำงานของระบบ
2. **ข้อกำหนดการจัดเรียง 8 ไบต์ตามฮาร์ดแวร์ (AAPCS Hardware Alignment Enforcement):**
สถาปัตยกรรม ARM Cortex-M บังคับให้ตำแหน่งตัวชี้สแต็ก (SP) ต้องจัดเรียงตรงกับพหุคูณของ 8 ไบต์เสมอ หากในขอบเขตย่อยมีการประกาศตัวแปรที่มีขนาดรวมไม่ลงตัวกับ 8 ไบต์ (เช่น `char` 1 ไบต์ หรือ `int` 1 ตัวขนาด 4 ไบต์) อัลกอริทึมจะส่งผ่านขนาดเข้าสู่ตัวดำเนินการ $\mathrm{Align8}(x)$ ทันที ทำให้ขนาดเฟรม $N$ ถูกปัดขึ้นเป็นพหุคูณของ 8 ไบต์ (เช่น ปัดจาก 4 ไบต์ เป็น 8 ไบต์) ก่อนสร้างคำสั่งปรับระดับสแต็ก เพื่อป้องกันการเกิด `UsageFault: Unaligned Stack Access` บนฮาร์ดแวร์
3. **การควบคุมลำดับสายการทำงานแบบข้ามขอบเขต (Early Exit Control Flow: `return` / `break` / `continue`):**
หากภายในขอบเขตย่อยซ้อนกัน (Nested Scopes) มีคำสั่งย้ายสายการทำงานออกก่อนจบขอบเขต เช่น `return` หรือ `break` คอมไพเลอร์จะคำนวณผลรวมของขนาดสแต็กในขอบเขตย่อยที่เปิดค้างอยู่ทั้งหมด ณ จุดนั้น ($\sum N_{\mathrm{nested}}$) และทำการแทรกคำสั่งคืนสแต็กสะสม `ADD SP, SP, #N_total` ล่วงหน้าโดยอัตโนมัติ ก่อนส่งออกคำสั่งควบคุมสาขา (`BX LR` หรือ `B`) เพื่อป้องกันอาการสแต็กรั่วไหล (Stack Accumulation Leak) และความล้มเหลวของตำแหน่ง SP เมื่อออกจากฟังก์ชัน
4. **ข้อจำกัดขนาดค่าคงที่ของคำสั่ง Thumb-2 (Immediate Encoding Overflow):**
ชุดคำสั่ง ARM Thumb-2 มีข้อจำกัดขนาดของค่าคงที่ (Immediate Value) สำหรับคำสั่ง `ADD`/`SUB` แบบไบต์เดี่ยว หากขอบเขตมีการจัดสรรตัวแปรขนาดใหญ่มากจนค่า $N$ เกินขีดจำกัดของการเข้ารหัส Thumb-2 Immediate ตัวสร้างโค้ดจะทำการเปลี่ยนรูปแบบเป็น Multi-instruction Sequence โดยโหลดค่า $N$ เข้าสู่ Scratch Register ชั่วคราว (เช่น เรจิสเตอร์ `R12` / IP) ก่อนทำการปรับสแต็กด้วยคำสั่ง `ADD SP, SP, R12` แทน
5. **ความเสถียรต่อสภาวะแทรกซ้อน (Interrupt Service Routines - ISR Alignment Immunity):**
ในระบบ Bare-Metal เหตุการณ์ Hardware Interrupt สามารถเกิดขึ้นได้ตลอดเวลา การบังคับใช้กฎ $\mathrm{Align8}(x)$ ในทุกๆ จุดเข้า-ออกขอบเขตอย่างเข้มงวด รับประกันได้ว่าค่า SP ในทุกคำสั่งจะพร้อมสำหรับการเกิด Context Stacking ของฮาร์ดแวร์ ARM Cortex-M เสมอ โดยไม่ทำให้กระบวนการตอบสนองต่อสัญญาณขัดจังหวะล้มเหลว

---

### 4. สมานตศาสตร์รูปนัยและการพิสูจน์คุณสมบัติคงตัว (Formal Semantics and Invariant Proofs)

เพื่อรับประกันความสามารถในการคาดเดาของการประมวลผลและความถูกต้องของสถานะสแต็กทางคณิตศาสตร์บนสถาปัตยกรรม ARM Cortex-M เราได้จัดรูปแบบคณิตศาสตร์แบบเป็นระบบเดียวผ่าน 7 ระบบสมการรูปนัย (Formulas 1–7)

#### 4.1 กรอบสมการรูปนัย 7 ข้อ (The 7-Equation Theoretical Framework)

$$\begin{aligned} 
&\mathbf{1.\ Small\text{-}Step\ Operational\ Semantics\ State\ Tuple} \\ 
&\quad \mathcal{S} \in \text{AST State}, \quad \sigma \in \text{Memory Store}, \quad sp \in \mathbb{N} \pmod 8 \\ 
&\quad \langle \mathcal{S}, \sigma, sp \rangle \longrightarrow \langle \mathcal{S}', \sigma', sp' \rangle \\ \\ 
&\mathbf{2.\ Hardware\ 8\text{-}Byte\ Alignment\ Operator} \\ 
&\quad \mathrm{Align8}(x) \triangleq (x + 7) \land \neg 7 \\ 
&\quad \forall x \in \mathbb{N}, \quad \mathrm{Align8}(x) \equiv 0 \pmod 8 \\ \\ 
&\mathbf{3.\ Frame\ Allocation\ Metric\ Function} \\ 
&\quad \mathrm{SizeOfScope}(B) \triangleq \mathrm{Align8}\left( \sum_{v \in \mathrm{Vars}(B)} \mathrm{sizeof}(v) \right) \\ \\ 
&\mathbf{4.\ Transition\ Inference\ Rules\ for\ Scope\ Block\ } B \\ 
&\quad \text{[E-ENTER]} \quad \frac{N = \mathrm{SizeOfScope}(B)}{\langle \mathrm{Enter}(B), \sigma, sp_0 \rangle \longrightarrow \langle \mathrm{Body}(B), \sigma, sp_0 - N \rangle} \\ 
&\quad \text{[E-EXEC]} \quad \frac{\langle \mathrm{Body}(B), \sigma, sp_0 - N \rangle \longrightarrow^* \langle \text{skip}, \sigma', sp_0 - N \rangle}{\text{Induction Hypothesis on Inner AST Nodes}} \\ 
&\quad \text{[E-EXIT]} \quad \frac{N = \mathrm{SizeOfScope}(B)}{\langle \mathrm{Exit}(B), \sigma', sp_0 - N \rangle \longrightarrow \langle \text{skip}, \sigma', (sp_0 - N) + N \rangle} \\ \\ 
&\mathbf{5.\ Theorem\ 1:\ Scope\ Stack\ Preservation\ Invariant} \\ 
&\quad \forall B \in \text{AST}, \quad \langle B, \sigma, sp_0 \rangle \longrightarrow^* \langle \text{skip}, \sigma', sp_{\text{exit}} \rangle \implies sp_{\text{exit}} = sp_0 \\ 
&\quad \mathbf{Proof\ Step:} \quad sp_{\text{exit}} = (sp_0 - N) + N = sp_0 \quad \blacksquare \\ \\ 
&\mathbf{6.\ Lemma\ 1:\ Hardware\ Alignment\ Preservation} \\ 
&\quad \forall sp_0 \equiv 0 \pmod 8, \quad \forall B \in \text{AST}: \\ 
&\quad \quad sp_{\text{mid}} = sp_0 - \mathrm{SizeOfScope}(B) \implies sp_{\text{mid}} \equiv 0 \pmod 8 \quad \blacksquare \\ \\ 
&\mathbf{7.\ Lemma\ 2:\ Scope\ Memory\ Isolation\ Bound} \\ 
&\quad \forall v \in \mathrm{Vars}(B), \quad \mathrm{Addr}(v) \in [sp_0 - \mathrm{SizeOfScope}(B),\ sp_0) \\ 
&\quad \quad \therefore \mathrm{Addr}(v) \cap [sp_0, \infty) = \emptyset \quad \blacksquare 
\end{aligned}$$

#### 4.1.1 รูปนัยทางคณิตศาสตร์และการพิสูจน์รายข้อ

1. **Small-Step Operational Semantics State Tuple**

$$\langle \mathcal{S}, \sigma, sp \rangle \longrightarrow \langle \mathcal{S}', \sigma', sp' \rangle$$



*การพิสูจน์:* กำหนดให้สถานะของระบบประกอบด้วย ทรีของสถานะ $\mathcal{S}$, หน่วยความจำ $\sigma$, และตัวชี้สแต็ก $sp$ การเปลี่ยนสถานะแบบ Small-step รับประกันว่าทุกการย้ายโหนดใน AST จะแปรผันแบบ Deterministic บนเซตของสถานะโดยไม่มีผลข้างเคียงพฤติกรรมที่ไม่ระบุ (Undefined Behavior) จากภายนอก
2. **Hardware 8-Byte Alignment Operator**

$$\mathrm{Align8}(x) \triangleq (x + 7) \land \neg 7$$



*การพิสูจน์:* เนื่องจาก $\neg 7$ ในระบบบิต Complement คือค่าบิตที่มี 3 บิตต่ำสุดเป็น `000` (binary `...11111000`) การนำ $(x + 7)$ ไปปฏิบัติการ Bitwise AND กับ $\neg 7$ จะทำให้ 3 บิตต่ำสุดของผลลัพธ์กลายเป็น 0 เสมอ ซึ่งค่าใดๆ ที่มี 3 บิตต่ำสุดเป็น 0 ย่อมหารด้วย $2^3 = 8$ ลงตัว ($\equiv 0 \pmod 8$)
3. **Frame Allocation Metric Function**

$$\mathrm{SizeOfScope}(B) \triangleq \mathrm{Align8}\left( \sum_{v \in \mathrm{Vars}(B)} \mathrm{sizeof}(v) \right)$$



*การพิสูจน์:* ให้ $V = \sum \mathrm{sizeof}(v)$ เป็นผลรวมขนาดหน่วยความจำจริงของตัวแปรในขอบเขต $B$ เมื่อผ่านตัวดำเนินการ $\mathrm{Align8}(V)$ ผลลัพธ์ $N = \mathrm{SizeOfScope}(B)$ จะเป็นขนาดเฟรมสแต็กที่หารด้วย 8 ลงตัวเสมอ ทำให้การปรับตำแหน่ง SP ไม่ทำลายการจัดเรียงของฮาร์ดแวร์ ARM Cortex-M
4. **Transition Inference Rules for Scope Block B**
แสดงการเปลี่ยนสถานะแบบเป็นลำดับขั้น: `[E-ENTER]` ลดค่า $sp$ ลง $N$ เพื่อจองสแต็ก `[E-EXEC]` ประมวลผลภายในขอบเขตโดยคงค่า $sp$ ไว้ และ `[E-EXIT]` คืนค่า $sp$ กลับด้วยการบวก $N$ เข้าไปตรงๆ ทำให้ได้สถานะสุทธิของการจองและคืนพื้นที่หน่วยความจำที่สมบูรณ์
5. **Theorem 1: Scope Stack Preservation Invariant**

$$\forall B \in \text{AST}, \quad \langle B, \sigma, sp_0 \rangle \longrightarrow^* \langle \text{skip}, \sigma', sp_{\text{exit}} \rangle \implies sp_{\text{exit}} = sp_0$$



*การพิสูจน์:* เริ่มต้นด้วย $sp = sp_0 \xrightarrow{[\text{E-ENTER}]} sp_1 = sp_0 - N \xrightarrow{[\text{E-EXEC}]} sp_2 = sp_0 - N \xrightarrow{[\text{E-EXIT}]} sp_{\text{exit}} = sp_2 + N$
ดังนั้น $sp_{\text{exit}} = (sp_0 - N) + N = sp_0$
สรุปได้ว่าเมื่อออกจากขอบเขตใดๆ ค่าตัวชี้สแต็กจะกลับคืนสู่ตำแหน่งเดิมเสมอ $\blacksquare$
6. **Lemma 1: Hardware Alignment Preservation**

$$\forall sp_0 \equiv 0 \pmod 8 \implies sp_{\text{mid}} \equiv 0 \pmod 8$$



*การพิสูจน์:* กำหนดให้ $sp_0 = 8k$ และจากสมการที่ 3 ได้ว่า $N = 8m$ ดังนั้น $sp_{\text{mid}} = 8k - 8m = 8(k - m)$ ส่งผลให้ $sp_{\text{mid}} \equiv 0 \pmod 8$ เสมอ ไม่เกิดอาการ Unaligned Stack Fault บน ARM Hardware $\blacksquare$
7. **Lemma 2: Scope Memory Isolation Bound**

$$\forall v \in \mathrm{Vars}(B), \quad \mathrm{Addr}(v) \in [sp_0 - \mathrm{SizeOfScope}(B),\ sp_0)$$



*การพิสูจน์:* สำหรับตัวแปร $v$ ใดๆ ในขอบเขต $B$ ตำแหน่งของตัวแปรคือ $\mathrm{Addr}(v) = sp_0 - N + \delta_v$ โดย $0 \le \delta_v < N$ ขอบเขตบนสูงสุดคือ $sp_0 - 1 < sp_0$ ดังนั้น ขอบเขตหน่วยความจำของตัวแปรใน $B$ ไม่มีทางลุกลามเข้าไปในพื้นที่สแต็กภายนอก $[sp_0, \infty)$ ได้อย่างสมบูรณ์ $\blacksquare$

#### 4.2 บทอธิบายรายละเอียดการพิสูจน์เชิงรูปนัย

จากลำดับกฎการเปลี่ยนสถานะ การสั่งคืนพื้นที่ด้วยคำสั่งระดับฮาร์ดแวร์ ARM Thumb-2 (`ADD SP, SP, #N`) ณ จุดสิ้นสุดขอบเขต AST ทำให้กระบวนการบริหารจัดการหน่วยความจำบนสแต็กมีความซับซ้อนในระดับเวลาเป็น $O(1)$ แบบคงที่อย่างสมบูรณ์ ปราศจากการเรียกใช้โค้ดรันไทม์ หรือการคำนวณภาระส่วนเกิน (Overhead) ที่ไม่สามารถคาดเดาเวลาได้

#### 4.3 การเชื่อมต่อ Middle-End แบบอนุรักษ์ข้อกำหนด (Middle-End Optimization and Invariant Preservation Rules)

ความแตกต่างทางปรัชญาสำคัญของ Kaoru Compiler คือความสัมพันธ์ระหว่าง Front-end และ Middle-end optimizer

แตกต่างจากสถาปัตยกรรมคอมไพเลอร์แบบดั้งเดิมที่ Middle-end อนุมานวงจรชีวิตของหน่วยความจำผ่านกราฟการไหลของโปรแกรม สถาปัตยกรรมของเรากำหนดให้ AST ในส่วน Front-end เป็นผู้ระบุเจตนาที่มีอำนาจเด็ดขาดสำหรับการคืนพื้นที่สแต็ก ส่วน Middle-end ทำหน้าที่เป็นเพียงตัวปรับแต่งที่อนุรักษ์ข้อกำหนดอย่างเข้มงวด โดยปรับปรุงความเร็วการประมวลผลในขณะที่ยังคงรักษากรอบโครงสร้างเชิงประจักษ์ที่กำหนดโดย AST

หากกระบวนการปรับแต่งใน Middle-end (เช่น Loop unrolling, Dynamic Scope Flattening หรือ Instruction rescheduling) พยายามจัดลำดับคำสั่งใหม่ข้ามขอบเขต `AST_SCOPE_BLOCK` หรือส่งผลกระทบต่อกรณีขอบเขตพิเศษ (Edge Cases) การแปลงนั้นจะถูกปฏิเสธ (Optimization Fallback) ทันที เว้นแต่จะสามารถพิสูจน์ทางคณิตศาสตร์ได้ว่า **Theorem 1** ($sp_{\text{exit}} = sp_0$) และ **Lemma 1** ($sp \equiv 0 \pmod 8$) ยังคงได้รับการรักษาไว้อย่างเคร่งครัด

---

### กิตติกรรมประกาศ (Acknowledgment)

ผู้เขียนขอขอบคุณชุมชนวิศวกรรมคอมไพเลอร์โอเพนซอร์ส และผู้พัฒนา QEMU และ GNU Toolchains สำหรับการจัดหาเครื่องมือวิจัยเปิดที่มีความเสถียรและทรงประสิทธิภาพ

---

### เอกสารอ้างอิง (References)

1. ARM Limited, "ARMv7-M Architecture Reference Manual," Document No. ARM DDI 0403E.b, 2018.
2. International Organization for Standardization, "Road vehicles --- Functional safety --- Part 6: Product development at the software level," ISO Standard 26262-6:2018, 2018.
3. B. Stroustrup, *The C++ Programming Language*, 4th ed. Addison-Wesley Professional, 2013.
4. R. Stallman and the GCC Developer Community, "Using the GNU Compiler Collection (GCC)," Free Software Foundation, 2022.
5. N. Matsakis and F. Klock, "The Rust Language," *ACM Queue*, vol. 12, no. 11, pp. 20--31, Nov. 2014.
6. F. Bellard, "QEMU, a Fast and Portable Dynamic Translator," in *Proc. USENIX Annual Technical Conference (ATC)*, 2005, pp. 41--46.

```

```
